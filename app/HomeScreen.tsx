import { View, Text } from 'react-native';

export default function HomeScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-text-primary text-xl font-bold">Inicio</Text>
      <Text className="text-text-secondary mt-2">Feed de productos — Paso 5</Text>
    </View>
  );
}
